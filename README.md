# Greenways Homebrew Tap

Homebrew formulae for tagged, installable Greenways AI open-source command-line tools.

Development is staged through pull requests. Once the repository is public, users will be able to install formulae with fully qualified names such as:

```sh
brew install greenways-ai/tap/hoplite
```
